#version 400 core

in vec3 lightVector;
in vec3 surfaceNormal;

out vec4 out_color;

uniform vec3 lightColor;

void main(void){
	float val = dot(lightVector, surfaceNormal);
	float brightness = max(val, 0.0);
	vec3 color = brightness * lightColor;
	
    out_color = vec4(color, 1.0);
}